"use client"

// Placeholder - react-resizable-panels not installed
export const ResizablePanelGroup = (props: any) => <div {...props} />
export const ResizablePanel = (props: any) => <div {...props} />
export const ResizableHandle = (props: any) => <div {...props} />
